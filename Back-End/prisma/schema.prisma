generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model ChatHistory {
  id        String   @id
  title     String
  createdAt DateTime @default(now())
  updatedAt DateTime
}
